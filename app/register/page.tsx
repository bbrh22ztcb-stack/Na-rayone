'use client';
import{useState}from'react';
import{useRouter}from'next/navigation';
export default function Register(){const[f,s]=useState({name:'',email:'',password:'',birthDate:'1990-01-01',place:'Гальнево'}),[x,sX]=useState('');const r=useRouter();const c=(k:string,v:string)=>s({...f,[k]:v});async function go(e:any){e.preventDefault();const q=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(q.ok)r.push('/profile');else sX((await q.json()).error)}return <div className="card" style={{maxWidth:620,margin:'20px auto'}}><h1>Регистрация</h1><form className="form" onSubmit={go}><div className="two"><div className="field"><label>Имя</label><input required value={f.name} onChange={e=>c('name',e.target.value)}/></div><div className="field"><label>Email</label><input required type="email" value={f.email} onChange={e=>c('email',e.target.value)}/></div></div><div className="two"><div className="field"><label>Пароль</label><input required minLength={8} type="password" value={f.password} onChange={e=>c('password',e.target.value)}/></div><div className="field"><label>Дата рождения</label><input required type="date" value={f.birthDate} onChange={e=>c('birthDate',e.target.value)}/></div></div><div className="field"><label>Населённый пункт</label><input value={f.place} onChange={e=>c('place',e.target.value)}/></div>{x&&<p style={{color:'var(--danger)'}}>{x}</p>}<button className="btn primary">Зарегистрироваться</button></form></div>}
