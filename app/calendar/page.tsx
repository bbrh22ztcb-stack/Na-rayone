import{db}from'@/lib/db';
import{currentUser}from'@/lib/auth';
export default async function Calendar(){const u=await currentUser();const [personal,events]=await Promise.all([u?db.calendarEntry.findMany({where:{userId:u.id},orderBy:{startsAt:'asc'}}):Promise.resolve([]),db.event.findMany({where:{status:'ACTIVE',startsAt:{gte:new Date()}},orderBy:{startsAt:'asc'},take:50})]);return <div className="stack"><div className="card"><h1>Календарь района</h1><p className="muted">Месяц · Неделя · День — базовая версия календаря. Публичные события синхронизируются автоматически.</p><div className="row"><button className="btn primary">Месяц</button><button className="btn">Неделя</button><button className="btn">День</button></div></div><div className="grid"><section className="card"><h2>Ближайшие события</h2><div className="list">{events.map(e=><div className="item" key={e.id}><b>{e.title}</b><div>{new Date(e.startsAt).toLocaleString('ru-RU')}</div><div className="muted">{e.placeName}</div></div>)}</div></section><section className="card"><h2>Мои записи</h2>{!u?<p className="muted">Войдите, чтобы видеть личный календарь.</p>:personal.length?<div className="list">{personal.map(x=><div className="item" key={x.id}><b>{x.title}</b><div>{new Date(x.startsAt).toLocaleString('ru-RU')}</div></div>)}</div>:<div className="empty">Пока пусто</div>}</section></div></div>}
