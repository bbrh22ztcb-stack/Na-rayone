import{db}from'@/lib/db';
import{currentUser}from'@/lib/auth';
import Join from'@/components/Join';
export default async function EventPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const e=await db.event.findUnique({where:{id},include:{organizer:true,attendances:{include:{user:true}}}});if(!e)return <div className="empty">Событие не найдено</div>;const u=await currentUser();const a=u?e.attendances.find(x=>x.userId===u.id):null;return <div className="stack"><div className="card"><span className="pill">Мероприятие</span><h1>{e.title}</h1><p>{e.description}</p><p><b>{new Date(e.startsAt).toLocaleString('ru-RU')}</b> — {new Date(e.endsAt).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}</p><p>📍 {e.placeName}</p><p className="muted">Организатор: {e.organizer.name}</p><Join eventId={e.id} status={a?.status}/></div><div className="card"><h2>Участники ({e.attendances.length})</h2><div className="row">{e.attendances.map(x=><span className="pill" key={x.id}>{x.user.name} · {x.status==='GOING'?'иду':'возможно'}</span>)}</div></div></div>}
